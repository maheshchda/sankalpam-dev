[phases.setup]
nixPkgs = ["nodejs_20", "npm"]

[phases.install]
# Combined commands into one single install phase
cmds = ["npm install --include=dev"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"
