[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install --include=dev"]

[phases.build]
cmds = ["npm run build"]
# This ensures we don't try to cache individual files as directories
cacheDirectories = ["node_modules/.cache", ".next/cache"]

[start]
cmd = "npm run start"
