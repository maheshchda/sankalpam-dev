[phases.setup]
nixPkgs = ["nodejs_20", "npm"]

[phases.install]
cmds = ["npm install --include=dev"]

[phases.build]
# We explicitly override the build phase to stop the broken cache mount
cmds = ["npm run build"]
cacheDirectories = ["node_modules/.cache", ".next/cache"]

[start]
cmd = "npm run start"
